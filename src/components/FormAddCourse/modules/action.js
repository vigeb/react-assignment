export const actSubmitCourse = (updateMode) => {
  return (dispatch) => {
    // ...
    if (updateMode) {
      // gọi api cập nhật khóa học
    } else {
      // gọi api tạo mới khóa học
    }
  }
}